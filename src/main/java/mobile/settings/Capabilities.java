package mobile.settings;

import org.openqa.selenium.remote.DesiredCapabilities;
/*
public class Capabilities {

    private static DesiredCapabilities getAndroidCapabilities() {
        DesiredCapabilities capabilities = new DesiredCapabilities();
        capabilities.setBrowserName(StringUtils.EMPTY);
        capabilities.setCapability(PLATFORM_NAME, "Android");
        capabilities.setCapability(PLATFORM_VERSION, "1.0");
        capabilities.setCapability(DEVICE_NAME, "fe8pcqxs5d49mr45");
        capabilities.setCapability(APP, "/home/kir/Apktest/screenshot-easy_4.1.6(29).apk");
        return capabilities;
    }
}*/